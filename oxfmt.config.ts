import { defineConfig } from "oxfmt";
// These are the defaults recommended by oxfmt
export default defineConfig({
  $schema: "./node_modules/oxfmt/configuration_schema.json",
  ignorePatterns: [],
});
